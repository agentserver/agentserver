// Package harnesslayout defines the fixed local attempt directory contract
// shared by harness-worker and the trusted harness-pool finalizer.
package harnesslayout

const (
	WorkerRuntimeDirectory = ".worker-runtime"
	AppRuntimeDirectory    = ".app-runtime"
	CodexHomeDirectory     = "codex-home"
)
