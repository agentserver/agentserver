// Package browsergateway exposes a stateless AG-UI projection of core-owned,
// committed canonical run events. It does not connect directly to Codex or own
// session, run, event, or approval state.
package browsergateway
