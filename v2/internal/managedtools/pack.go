// Package managedtools defines the shared identity of the closed-world
// managed CLI pack. Provider-specific credential and command policies remain
// in their own packages; this ID binds the combined model instructions and
// runtime artifacts carried by one run manifest.
package managedtools

const PackID = "managed-cli-readonly@v1"
